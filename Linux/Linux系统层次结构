Linux系统层次结构：
从外到内；
	应用程序<---->Linux操作系统<---->硬件
				（Shell<-->Kernel内核）

1、Shell：命令解释器，介于用户和内核之间，提供用户与系统命令的交互
			shell本身也是一个进程，表现形式：终端，命令行窗口
	①常见shell类型：bash,csh,sh；
	②查看当前是什么shell，使用命令ps（查看当前部分进程）；
	③
